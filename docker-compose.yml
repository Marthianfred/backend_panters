version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: panters_backend
    ports:
      - '3001'
    environment:
      - NODE_ENV=production
    restart: unless-stopped
